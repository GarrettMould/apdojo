'use client';

import React from 'react';
import { QuestionsGrid } from '@/components/questionsGrid'; // Ensure lowercase g

export default function AiFrqHelperPage() {
  return (
    <div>
      {/* You might need to pass props to QuestionsGrid depending on its definition */}
      <QuestionsGrid />
    </div>
  );
}
