import type { Question } from '@/data/questionBanks/types';

/**
 * AP U.S. Government & Politics — Unit 4 MCQ **practice** pool.
 * Practice IDs: 6117–6156. Merged via `govMcqPractice.ts` → `allQuestions`.
 *
 * Unit 4 **formal unit test** uses `govUnit4McqTestQuestions` in `govUnit4McqExam.ts` (separate bank).
 */
export const govUnit4McqPracticeQuestions: Question[] = [];
